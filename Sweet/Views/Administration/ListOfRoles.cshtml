@model IEnumerable<IdentityRole>

<h1> List of Roles</h1>

@if(Model.Any())
{

}
else
{
@Html.BeginForm("CreateRole", "Administration")
    <button type = "submit" class="btn btn-default"> Make New Role</button>
}