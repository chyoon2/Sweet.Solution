@model EditRoleViewModel
@using Sweet.ViewModels

<h1>Update Role<h1>

  @Html.LabelFor(model=> model.Id): @Html.DisplayFor(model=>model.Id)
  
  <br>
  @Html.LabelFor(model=> model.RoleName):
  

@using (Html.BeginForm("Edit", "Administration"))
{
  @Html.HiddenFor(model => model.Id)
  @Html.EditorFor(model => model.RoleName)
  <button type = "submit" class="btn btn-default"> Update</button>
}

<h2>users in this role</h2>
 @if (Model.Users.Any())
  {
    foreach (var user in Model.Users)
  {
    <h5>@user</h5>
  }
  }
  else
  {
      <h5>None current users</h5>
  }

@using (Html.BeginForm("Edit", "Administration"))
{
 <button type="submit" class="btn btn-primary" >Remove Users</button>
}

 <button type="submit" class="btn btn-primary" >Add Users</button>